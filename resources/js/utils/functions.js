let displayPercentage = (number) => (number * 100).toFixed(1).replace('.', ',');

let displayAverage = (number) => (number * 1).toFixed(1).replace('.', ',');

export { displayAverage, displayPercentage };

